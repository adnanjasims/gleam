export function FramesAndSills() {
  return (
    <section
      id="frames-sills"
      className="border-y border-sky-100 bg-gradient-to-br from-sky-50 via-white to-slate-50 py-16 sm:py-20"
    >
      <div className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
        <div className="max-w-3xl">
          <p className="text-sm font-semibold uppercase tracking-[0.16em] text-accent">
            Complete window care
          </p>
          <h2 className="mt-2 font-display text-3xl font-bold tracking-tight text-navy sm:text-4xl">
            We clean glass, frames, and sills
          </h2>
          <p className="mt-4 text-base leading-relaxed text-muted sm:text-lg">
            Every window job includes the full frame-not just the glass. We
            clear dirt, pollen, and buildup from sills and frames so the whole
            window looks finished, not half-done.
          </p>
        </div>
      </div>
    </section>
  );
}
